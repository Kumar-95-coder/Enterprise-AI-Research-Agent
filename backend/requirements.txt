fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
alembic>=1.13
pydantic>=2.9
pydantic-settings>=2.6
scikit-learn>=1.5
numpy>=1.26
spacy>=3.8
requests>=2.32
python-dotenv>=1.0

# optional, only needed if SEARCH_PROVIDER=duckduckgo
ddgs>=9.0

# optional, only needed if LLM_PROVIDER=anthropic / openai respectively
anthropic>=0.40
openai>=1.50

# optional, only needed for BACKGROUND_MODE=celery
celery>=5.4
redis>=5.1

# test dependencies
pytest>=8.3
httpx>=0.27
